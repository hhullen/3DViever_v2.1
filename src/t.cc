#include <iostream>
#include <vector>

int main() {
  // std::vector<int> t0({1, 2, 3});
  // std::vector<int> t1({4, 5, 6});

  // for (int i = 0; i < t0.size(); ++i) {
  //   std::cout << t0[i] << "\n";
  // }
}